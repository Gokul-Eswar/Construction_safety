# MQTT Broker configuration
MQTT_HOST=localhost
MQTT_PORT=1883

# Dashboard API Configuration (Backend)
PORT=3001
CONFIG_PATH=../../config.json

# If using Docker Compose, these are usually handled automatically.
