# Specification: Documentation Update

## Context
The project has evolved significantly. It now supports Docker deployment, multiple camera streams, and has a completely new UI. The existing `readme.md` still describes the initial prototype setup.

## Goals
1.  **Update `readme.md`:**
    -   Highlight the new features (Multi-Stream, Docker, Modern UI).
    -   Replace the "Build Instructions" with the new "One-Click Start" instructions.
    -   Add a section on "Manual Configuration" for advanced users (Web UI).
2.  **Update `conductor/product.md`:**
    -   Reflect the current completed state of the product.

## Requirements
-   Clear, step-by-step instructions for end users.
-   Technical details for developers (Developer Guide section).
-   Screenshots references (placeholders) for the new UI.

## Deliverables
-   Updated `readme.md`
-   Updated `conductor/product.md`